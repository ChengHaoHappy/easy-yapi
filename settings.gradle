pluginManagement {
    repositories {
        // 插件使用阿里云 maven 源
        maven {
            url 'https://maven.aliyun.com/repository/gradle-plugin'
        }
    }
}

rootProject.name = 'easy-yapi'
include 'idea-plugin'
include 'common-api'
include 'plugin-adapter:plugin-adapter-markdown'
findProject(':plugin-adapter:plugin-adapter-markdown')?.name = 'plugin-adapter-markdown'

